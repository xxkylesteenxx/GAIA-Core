# GAIA-Meta — thin CI caller example.
# Copy this file to .github/workflows/ci.yml in the GAIA-Meta repo.
# Spec ref: GAIA Deployment and Attested Identity Spec v1.0 §8, DEP-004
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  meta-coordinator:
    uses: xxkylesteenxx/GAIA-Core/.github/workflows/reusable-build-and-publish.yml@main
    with:
      image:             gaia-meta-coordinator
      dockerfile:        docker/Dockerfile.python-core
      language:          python
      working_directory: src
      publish:           ${{ github.ref == 'refs/heads/main' }}
    secrets: inherit
