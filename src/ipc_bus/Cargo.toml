[package]
name    = "ipc_bus"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1"
log       = "0.4"
# serde     = { version = "1", features = ["derive"] }  # uncomment for typed schemas
