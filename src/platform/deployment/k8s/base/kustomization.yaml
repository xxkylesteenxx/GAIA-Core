# Kustomize base — compose all base resources.
# Spec ref: GAIA Deployment and Attested Identity Spec v1.0 §5.2
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - networkpolicy.yaml
  - deployment.yaml
  - service.yaml
