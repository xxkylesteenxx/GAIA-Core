# GAIA Kubernetes — namespace definition.
# Spec ref: GAIA Deployment and Attested Identity Spec v1.0 §5
apiVersion: v1
kind: Namespace
metadata:
  name: gaia
  labels:
    app.kubernetes.io/part-of: gaia
    gaia.io/managed: "true"
