# GAIA node configuration — generated by Ansible.
# DO NOT edit manually; re-run ansible-playbook to update.
# Spec ref: GAIA Deployment and Attested Identity Spec v1.0 §7

[node]
role        = {{ gaia_role }}
tag         = {{ gaia_node_tag }}
base_dir    = {{ gaia_base_dir }}
config_dir  = {{ gaia_config_dir }}
log_dir     = {{ gaia_log_dir }}

[identity]
spire_socket = {{ gaia_spire_socket }}
