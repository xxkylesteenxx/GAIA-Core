{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: gaia-secrets
  namespace: {{ .Values.namespace }}
type: Opaque
data: {}
# Populate via: helm install --set-string secrets.data.KEY=value
# or use an ExternalSecret / sealed-secret to manage this resource externally.
{{- end }}
